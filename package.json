{
  "name": "estudio-duarte-muinos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy",
    "firebase:login": "firebase login",
    "firebase:init": "firebase init"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "firebase-tools": "^13.35.1",
    "vite": "^5.0.0"
  }
}
